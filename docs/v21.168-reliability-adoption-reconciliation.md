# PHOENIX v21.168 — Reliability Adoption Reconciliation Authorization & Ordered Consumer Recovery Governance

## Purpose
Convert a human-approved `reconciliation-ready` drift plan into an ordered, separately approved consumer recovery sequence without mutating runtime consumers.

## Admission
Only v21.167 `reconciliation-ready` evidence with human approval may enter. Workspace and exact baseline ID/version/digest lineage remain bound.

## Lifecycle
`review-required -> authorized -> staged -> recovery-ready`

Any invalid source, missing lineage, consumer overlap, sequence mismatch/order violation, excessive blast radius/residual risk, missing human approval or Risk Brain hard block fails closed to `blocked`.

## Recovery sequencing
Every affected consumer appears exactly once in the deterministic sequence. Healthy consumers are preserved and excluded from recovery steps. Every step requires separate human approval and must be approved in order.

## Safety boundary
This module authorizes and records recovery intent only. It does not reconfigure or mutate consumers, activate a baseline, change routing/policy/credentials/permissions, move funds or submit orders.

## Next
v21.169 should add Reliability Recovery Receipt Reconciliation & Cross-Consumer Completion Governance, verifying each approved recovery step against fresh exact-match adoption receipts before coordinated recovery can be marked completed.
