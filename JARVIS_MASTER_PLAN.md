# JARVIS / AURON — Canonical Master Plan

Status: canonical project roadmap
Repository: `ScalpingEdward/JARVIS_os`

## 1. Mission
JARVIS is the master operating system. AURON is its controlled intelligence/orchestration layer. Vertical capabilities operate through one governed core, one Command Centre, shared identity/audit state, and explicit safety boundaries. Build sequence must be preserved.

## 2. Source-of-truth rule for every new chat/build session
1. Read this file from `main`.
2. Verify the last claimed PR is actually merged into `main`.
3. Inspect current implementation on `main`.
4. Continue from the checkpoint and dependency graph.
5. Never restart merged layers or invent merge state.
6. Reconcile implementation and roadmap explicitly if they disagree.

## 3. Architecture invariants
- JARVIS = master OS; AURON = orchestration/intelligence/governance.
- Command Centre remains operational UI with persistent command/text interaction.
- Consequential external actions require explicit execution boundaries, observable results, audit and failure state.
- Simulation remains independent of external execution.
- Provider adapters cannot bypass central policy/risk/approval controls.
- Idempotency, reconciliation, health, provenance/version state and fail-closed behavior are core infrastructure.
- Secrets never belong in source control.
- External execution is deliberate, never default.

## 4. Completed foundation lineage
Foundation successor governance closed at v21.523. Phase A core cutover is complete through A6.

## 5. Mandatory build sequence
### Phase B — Trading (reconciled)
The real, wired path is `backend/app/trading/api.py` -> `service.py` (`GET /v1/trading/status`, `POST /v1/trading/evaluate`, setup listing) registered in `main.py`. This is genuinely usable today.

Fourteen `auron_*_v21_53x` / `v21_604-607` modules in `backend/app/trading` and `backend/app/core` (registry, account state, signal intake, risk engine, multi-account allocation, guards, MT5 adapter, reconciliation canary, controlled live enablement, command centre, shadow-canary selection/adapter/certification/health) plus their `api/routes` command-centre file were audited the same way as the Research H21-H40 chain: never imported by `main.py`, never called by `trading/api.py` or `trading/service.py`, only importing and testing each other. They have been removed. Full suite green afterward (4615 passed).

**Note preserved during audit:** `backend/app/core/auron_integration_readiness_vNNN.py` files form a sequential linked chain (each version imports the previous one, `v21_539` -> `v21_538` -> ... -> `v21_1`) shared across every vertical, not a per-vertical ledger. Deleting a file in the middle of this chain breaks every higher version that has not itself been reconciled/removed yet. Do not delete an `auron_integration_readiness_vNNN.py` file unless every version above it in the chain has already been removed or repointed. This chain itself is a candidate for a future, carefully-sequenced cleanup, not an ad hoc deletion.

B1-B10 as a phase label is retired in favor of the concrete statement above: one real endpoint set exists and works; the rest was ceremony.
### Phase C — Instagram Content Manager
Completed through C8; provider writes remain gated.
### Phase D — Additional verticals
Communications D1-D8, Research D9-D16, Automation D17-D24 and Files & Documents D25-D32 completed.
### Phase E — Cross-vertical integration certification
E1-E4 completed.
### Phase F — Controlled provider canary program
F1-F4 completed.
### Phase G — Provider-specific controlled canary integration
Research G1-G4, Instagram G5-G8, Files & Documents G9-G12, Communications G13-G16 and Trading-shadow G17-G20 are complete. G21 completes the evidence-driven expansion decision.

### Phase H — Controlled external-provider sandbox integration (reconciled)
H1-H20 established a real, contract-bound, read-only sandbox adapter (no network transport, no credential resolution) and are retained.

H21-H40 (transport injection, authorization gates, execution-boundary design, and the H40 request-execution design contract) were audited against the actual repository and found to be fully disconnected: none of the ~50 modules in this range were imported by `main.py` or any other application code, they only imported and tested each other, and the one concrete "request" they modeled targeted a non-existent `sandbox.example.test` domain. No module in this range ever performed, or could perform, a network call. This range (32 research modules + 32 paired `core` integration-readiness modules + their tests, ~96 files) has been removed rather than carried forward, since it added no real capability and no real safety guarantee beyond what H1-H20 already provide.

**Replacement:** `backend/app/research/real_provider_client.py` + `backend/app/research/provider_api.py`, wired into `main.py` at `POST /v1/research/provider/fetch`. This is a real, executable client: HTTPS-only, explicit domain allowlist (currently `api.github.com`), bounded timeout, bounded response size, exactly-once per `request_id` via an append-only sqlite audit log, no write capability, no credential handling. Covered by `backend/tests/test_research_real_provider_client.py` (allowlist rejection, scheme rejection, size-limit enforcement, timeout handling, exactly-once, and the wired API endpoint), all passing alongside the full existing suite (4694 tests green after this change).

Phase H is considered functionally complete for a first real Research provider path. Extending the allowlist to additional domains/providers is a deliberate, reviewed code change to `DEFAULT_ALLOWED_DOMAINS`, never a runtime parameter.

**Lesson carried forward:** future phases must add one real, wired, testable capability per PR rather than a new design/certification layer describing a capability that is not yet wired in. If a PR's diff does not touch `main.py`'s router registration or an equivalent real entrypoint, it does not ship user-visible functionality and should say so explicitly rather than advancing the phase counter.

**Reconciliation (2026-08-30) — physical dead-code removal of `backend/app/research/auron_research_*`:** the roadmap above described the H21-H40 chain as "removed", but a fresh import-graph audit from `backend/app/main.py` found that 19 `auron_research_*_v21_5xx/6xx.py` modules were still physically present in `backend/app/research/` and fully unreachable. `main.py` wires only `research/provider_api.py` (+ `research/real_provider_client.py`) via `POST /v1/research/provider/fetch`; none of the 19 modules were imported by `main.py` or by any other reachable application module — they imported and tested only each other (a closed, self-referential cluster). All 19 modules and their 19 paired `backend/tests/test_auron_research_*` files were deleted in this PR. The full suite is green afterwards (4515 passed, 0 failed; 100 tests belonging to the removed cluster dropped from the prior 4615). No `backend/app/core/auron_integration_readiness_vNNN.py` file was touched — the sequential readiness chain (93 files) is preserved intact per the note above. Removed research modules: `adapter_onboarding_v21_556`, `registry_evidence_v21_557`, `read_search_fetch_v21_558`, `evidence_policy_v21_559`, `report_simulation_v21_560`, `controlled_watch_v21_561`, `watch_reconciliation_v21_562`, `command_centre_v21_563`, `readonly_canary_adapter_v21_588`, `canary_e2e_certification_v21_589`, `provider_health_drift_v21_590`, `canary_command_centre_v21_591`, `external_readonly_sandbox_adapter_v21_610`, `external_sandbox_e2e_reconciliation_v21_611`, `external_sandbox_health_drift_observability_v21_612`, `network_transport_authorization_v21_613`, `readonly_network_transport_boundary_v21_614`, `readonly_network_boundary_e2e_certification_v21_615`, `real_provider_activation_readiness_v21_616`.

**New capability (2026-08-30) — real, wired Trading Accounts & Strategy Assignment registry:** `backend/app/accounts/` (`models.py`, `service.py`, `api.py`) wired into `main.py` at prefix `/v1/accounts`. This is the first *persistent source of truth* for which accounts AURON manages and which strategies run on each — the foundation the vision needs ("manage prop-firm accounts, run 1-2 strategies per account, and also trade live accounts"). It follows the Research pattern: real sqlite persistence (`data/accounts.db`, gitignored), fail-closed validation, and an append-only `account_audit` trail for every mutation. Capabilities: register `prop`/`live`/`demo` accounts (prop accounts *require* a `PropFirmRules` set: max daily loss %, max total drawdown %, profit target %, min trading days, static/trailing drawdown); `(login, server)` uniqueness; assign strategies with a per-account cap (default 2, honouring the "1-2 strategies per account" rule), no duplicate `strategy_id`, and total allocation ≤ 100%; push broker state snapshots (`balance`/`equity`/`day_start_balance`) that auto-flag `breached` status on a prop rule violation and count distinct UTC trading days; compute live compliance (daily-loss / drawdown / profit headrooms, profit-target progress, breach reasons); and `suspend`/`activate` with breached accounts blocked from re-activation. Endpoints: `POST /v1/accounts`, `GET /v1/accounts`, `GET /v1/accounts/{id}`, `GET|POST /v1/accounts/{id}/strategies`, `DELETE /v1/accounts/{id}/strategies/{strategy_id}`, `POST /v1/accounts/{id}/state`, `GET /v1/accounts/{id}/compliance`, `POST /v1/accounts/{id}/suspend`, `POST /v1/accounts/{id}/activate`. **No order execution** is introduced here — this is deliberately the read/registry foundation; execution reuses the existing gated executor modules in a later PR. Covered by `backend/tests/test_accounts_registry.py` (22 tests: registration rules, uniqueness, strategy caps/allocation, compliance math, daily-loss/drawdown/trailing-drawdown breaches, trading-day counting, suspend/activate, persistence across re-init, and the wired API surface). Full suite green: 4267 passed, 0 failed (4245 + 22 new).

**New capability (2026-08-30) — MT5→Accounts state sync bridge:** `backend/app/account_state_sync/` wired into `main.py` at `/v1/account-state-sync`. Bridges the read-only MT5 bridge (`/v1/mt5`, which stores inbound snapshots from Windows/VPS MT5 terminals) and the persistent accounts registry (`/v1/accounts`). On-demand sync finds each registered account's MT5 terminal by exact `(login, server)` match, extracts the live broker snapshot (`balance`, `equity`), and pushes it into the registry via `POST /v1/accounts/{id}/state` where compliance checks and breach-detection run automatically. Syncs only `active` accounts by default; the `force` flag includes `suspended`/`breached` for manual recovery flows. The `account_ids` filter syncs a specific subset. Real-world flow: Windows/VPS runs `mt5_bridge_client.py` → pushes snapshots to `/v1/mt5/terminals/{id}/snapshot` every 5s → operator or scheduler calls `POST /v1/account-state-sync/sync` → accounts registry sees live broker state and flags breaches without any manual intervention. Endpoints: `GET /v1/account-state-sync/status`, `POST /v1/account-state-sync/sync`. Covered by `backend/tests/test_account_state_sync.py` (18 tests: matching/non-matching terminals, breach detection during sync, filtering by account_ids, force flag, multi-account sync, status tracking, API surface). Full suite green: 4285 passed, 0 failed (4267 + 18 new). This closes the live-observability loop — AURON now sees real broker account states and can auto-detect prop-firm rule breaches.

**New capability (2026-08-30) — Trading strategies infrastructure + scalping_3tp implementation:** `backend/app/strategies/` wired into `main.py` at `/v1/strategies`. This is the **trading brain** — ICT/SMC-based entry logic with multi-TP exits. Each strategy receives a `MarketSnapshot` (current price, HTF bias, Fair Value Gaps, Order Blocks, structure levels, session) and returns either a `TradingSetup` (entry, stop-loss, 3 TPs with partial-close percentages, risk-reward ratio, confidence score, reasoning) or a clear reason why no setup was generated. **Implemented strategies:** `scalping_3tp` — FVG + Order Block mitigation with 3 TPs at 30%/50%/100% RR, min 1:2 RR. Entry logic: Long requires HTF bullish + price mitigating bullish Order Block + bullish FVG; Short requires HTF bearish + price mitigating bearish Order Block + bearish FVG. SL placed below/above Order Block with 0.05% buffer. TPs calculated at 30%, 50%, and 100% of the risk-reward distance. Confidence score starts at 60% and increases 10% per RR point. Setup invalidated if SL is hit (structure break). **Second strategy `ict_silver_bullet`** (added 2026-08-31, `backend/app/strategies/ict_silver_bullet/`): session-gated to London/NY windows, FVG entry aligned with HTF bias, structure-based SL (nearest protective swing, FVG-boundary fallback) and nearest structure-liquidity target with 2 TPs (50%/100%), min 1:2 RR — deliberately uses the `session` + `structure_levels` snapshot fields `scalping_3tp` ignores, covered by `backend/tests/test_strategy_ict_silver_bullet.py` (19 tests). **Future strategies** (`smc`, `open_range`) follow the same pattern and will be added to the registry incrementally. Service layer (`strategies/service.py`) maintains a registry of all strategies with metadata (id, name, description, evaluate function) and provides methods to evaluate a single strategy, all strategies, or filter to only valid setups. Endpoints: `GET /v1/strategies/` (list all), `POST /v1/strategies/evaluate/{strategy_id}` (evaluate one), `POST /v1/strategies/evaluate-all` (evaluate all, including no-setups), `POST /v1/strategies/find-setups` (filter to only valid setups). Covered by `backend/tests/test_strategy_scalping_3tp.py` (15 tests: valid long/short setups, SL placement, TP calculation, confidence scoring, no-setup scenarios for neutral HTF/missing OB/missing FVG/wrong direction/price not mitigating/already mitigated structures, minimum RR enforcement, result model consistency) and `backend/tests/test_strategies_api.py` (16 tests: list strategies, evaluate single strategy with valid long/short/no-setup/unknown strategy/invalid snapshot, evaluate-all including no-setups, find-setups filtering, service layer unit tests). Full suite green: 4316 passed, 0 failed (4285 + 31 new). This is the **first real trading logic** — strategies generate actionable setups with precise entry/SL/TP levels. **No execution yet** — that comes in the next PR where we wire strategy assignments + account compliance + the existing approval-gated MT5 executor.

**New capability (2026-08-31) — Setup Submission bridge (orchestrator output → approval gate):** `backend/app/setup_submission/` (`__init__.py`, `models.py`, `service.py`, `api.py`) wired into `main.py` at `/v1/setup-submission`. This is the **bridge** that turns strategy output into approval requests, connecting the accounts registry (`/v1/accounts`) and the strategies brain (`/v1/strategies`) to the human-in-the-loop approval gate. Flow: `POST /v1/setup-submission/submit` accepts a full `MarketSnapshot` plus an optional `account_ids` filter; for every *executable* account (fail-closed: status must be exactly `active`, so `suspended`/`breached`/`passed` accounts are evaluated but never submit) it evaluates each of the account's *enabled* strategy assignments against the snapshot; every resulting `TradingSetup` becomes a `SubmittedSetup` (account_id, login, strategy_id, symbol, side, entry, SL, TPs, RR, confidence, reasoning) with a freshly generated `approval_request_id` (UUID4). It returns a `SetupSubmissionReport` (symbol, total_accounts_evaluated, total_executable_setups, total_submitted, submitted_setups, optional skipped_reason). **This never executes trades** — it only records pending approval requests for a human operator to resolve through the existing approval-gated execution chain (`auron_demo1_approval_handoff/resolution/…_v21_26x`). Approval requests are held **in memory** keyed by `approval_request_id` for now; a later PR will persist them into the real Phoenix `demo1_approval_inbox` format without changing this module's public contract. Endpoints: `POST /v1/setup-submission/submit`, `GET /v1/setup-submission/pending`, `GET /v1/setup-submission/pending/{approval_request_id}` (404 on unknown). The service's account-registry and strategy-service dependencies are injectable for isolated testing. Covered by `backend/tests/test_setup_submission.py` (18 tests: zero accounts, one account × one/two strategies, multiple accounts, breached/suspended accounts submit nothing, no-setup snapshot, unique approval IDs, account_ids filter, pending list/single/unknown lookup, independent repeat submits, symbol-mismatch rejection, and the wired `/submit` + `/pending` API surface). Full suite green: 4353 passed, 0 failed (4335 + 18 new). **No order execution is introduced here** — the operator still approves/rejects through the existing Phoenix inbox; the next PR wires approved setups into the gated MT5 executor.

## 6. Cross-vertical Command Centre requirements
Persistent command/text interaction, capability health, simulation/execution visibility, approvals, execution timeline/failures, dedicated vertical workspaces, kill switches and audit/evidence access remain mandatory.

## 7. Definition of usable
A vertical requires observable provider health, persistent state, policy before outbound action, idempotency/reconciliation, visible results, disable controls, failure/replay tests and deliberate external enablement.

## 8. PR discipline
One coherent layer per PR, with tests, dependencies and next layer documented. Verify merge before advancing.

## 9. Current checkpoint
- Foundation through v21.523 and Phase A A1-A6: complete.
- Trading B1-B10, Instagram C1-C8 and Phase D vertical architecture: complete with consequential provider execution gated.
- Phase E E1-E4, Phase F F1-F4 and Phase G through G21: complete.
- H1-H20 complete: Research provider path is contract-bound, reconciled, certified through one bounded transport-disabled session and zero-concrete-transport injection contract.
- H21-H31 complete: governed transport activation, authorization, binding, injection design/certification and exactly one inert read-only transport object are established and certified with network execution disabled.
- H32-H35 complete: one short-lived operator-bound revocable one-shot network-execution authorization is designed, certified, issued and certified again against exact transport/object/identity scope while provider traffic remains disabled.
- H36-H37 complete: the fail-closed exactly-once consumption boundary is designed and certified with expiry/revocation/read-only scope and zero network state.
- H21-H40 (32 research modules + 32 paired core modules + tests) removed: confirmed disconnected from `main.py` and from each other's non-test callers, tested only against a non-existent sandbox domain, and never capable of a real network call. Removal verified against the full test suite (4694 passed, 0 failed).
- **Research provider path is now real and wired**: `POST /v1/research/provider/fetch` performs an actual bounded HTTPS GET against an explicit domain allowlist, with timeout, response-size limit, exactly-once request handling and an append-only audit log. This supersedes the H21-H40 design chain.
- **Dead-code reconciliation (2026-08-30):** 19 unreachable `backend/app/research/auron_research_*` modules that the roadmap had already described as removed but were still on disk, plus their 19 paired `backend/tests/test_auron_research_*` files, have now been physically deleted (branch `chore/remove-dead-auron-research-modules`). Verified unreachable via an import-graph audit from `backend/app/main.py`; no `auron_integration_readiness_vNNN.py` file touched. Full suite green: 4515 passed, 0 failed (down from 4615 as the 100 tests of the removed cluster were deleted).
- **Multi-vertical dead-code reconciliation (2026-08-30):** A comprehensive import-graph audit from `backend/app/main.py` identified 47 additional unreachable `auron_*` modules across multiple verticals (8 automation, 11 communications, 3 content/instagram, 14 core, 11 documents), all confirmed disconnected from the live application. These modules, plus their 47 paired test files (94 files total), have been physically deleted (branch `chore/remove-dead-auron-modules-multi-vertical`). No `auron_integration_readiness_vNNN.py` file touched — the sequential readiness chain remains intact. Full suite green: 4245 passed, 0 failed (down from 4515 as 270 tests of the removed clusters were deleted).
- **Trading Accounts & Strategy Assignment registry is now real and wired (2026-08-30):** `backend/app/accounts/` at prefix `/v1/accounts` (branch `feat/trading-accounts-registry`) — the first persistent registry of the prop/live/demo accounts AURON manages and the (1-2) strategies assigned to each, with prop-firm rule enforcement, sqlite persistence, an append-only audit trail, and live compliance/breach computation. Read/registry foundation only; no order execution. Full suite green: 4267 passed, 0 failed (4245 + 22 new). This is the concrete first step toward the account-management vision and precedes any execution wiring.
- **MT5→Accounts state sync bridge is now real and wired (2026-08-30):** `backend/app/account_state_sync/` at `/v1/account-state-sync` (branch `feat/account-state-sync`) — bridges the read-only MT5 bridge and the accounts registry. Matches each registered account to its MT5 terminal by `(login, server)`, extracts live `balance`/`equity` snapshots, pushes into the registry where compliance/breach-detection runs automatically. The live-observability loop is now closed: Windows/VPS → MT5 bridge → state-sync → accounts registry sees real broker state and auto-flags prop-firm breaches. Full suite green: 4285 passed, 0 failed (4267 + 18 new).
- **Trading strategies infrastructure + scalping_3tp is now real and wired (2026-08-30):** `backend/app/strategies/` at `/v1/strategies` (branch `feat/trading-strategies`) — the first **trading brain** with ICT/SMC-based entry logic. Strategy registry pattern (same as accounts): each strategy evaluates a `MarketSnapshot` → returns `TradingSetup` (entry, SL, 3 TPs, RR, confidence) or a clear no-setup reason. `scalping_3tp` implemented: FVG + Order Block mitigation, 3 TPs at 30%/50%/100% RR, min 1:2 RR. Service layer manages the strategy registry and provides evaluate-one / evaluate-all / find-setups methods. API exposes list/evaluate/evaluate-all/find-setups endpoints. Full suite green: 4316 passed, 0 failed (4285 + 31 new). **This is the first real trading logic** — strategies generate actionable setups with precise entry/SL/TP levels.
- **ICT Silver Bullet strategy added to the registry (2026-08-31):** `backend/app/strategies/ict_silver_bullet/` (branch `feat/strategy-ict-silver-bullet`) — the second strategy on the shared infrastructure, proving the registry's extensibility. Deliberately exercises the `session` and `structure_levels` snapshot fields that `scalping_3tp` ignores: session-gated to the London/NY "Silver Bullet" windows only, takes entries into an unmitigated FVG aligned with HTF bias, places the SL beyond the nearest protective swing structure (falls back to the FVG boundary with a 0.05% buffer when no structure exists), and targets the nearest opposing structure level (liquidity) with 2 TPs (50% at 50% RR, remainder at full target), min 1:2 RR. Confidence heuristic: base 55% + 10%/RR point + 10% structure-alignment bonus. Registered in `strategies/service.py` alongside `scalping_3tp`; no new endpoints or shared-model changes needed. Full suite green: 4335 passed, 0 failed (4316 + 19 new; two pre-existing api tests that assumed a single-strategy registry were generalised to assert both strategies).
- **Setup Submission bridge is now real and wired (2026-08-31):** `backend/app/setup_submission/` at `/v1/setup-submission` (branch `feat/setup-submission`) — the bridge from orchestrator/strategy output to the human approval gate. `POST /submit` takes a `MarketSnapshot` (+ optional `account_ids` filter), evaluates each **active** account's **enabled** strategy assignments, and records one pending approval request per resulting setup (each with a UUID4 `approval_request_id`, full context: account_id/login/strategy_id/symbol/side/entry/SL/TPs/RR/confidence/reasoning); returns a `SetupSubmissionReport`. `GET /pending` and `GET /pending/{id}` expose the pending requests (404 on unknown). Fail-closed: `suspended`/`breached`/`passed` accounts are evaluated but never submit. **No trade execution** — approvals are held in memory pending a later PR that persists them into the Phoenix `demo1_approval_inbox` format and wires approved setups into the gated MT5 executor. Covered by `backend/tests/test_setup_submission.py` (18 tests). Full suite green: 4353 passed, 0 failed (4335 + 18 new).
- **Strategy Orchestrator — accounts + strategies + compliance integrated (2026-08-31):** `backend/app/strategy_orchestrator/` at `/v1/strategy-orchestrator` (branch `feat/strategy-orchestrator`) — brings together the three building blocks (accounts registry, strategies, compliance). For each active account, evaluates only the strategies assigned to that account, checks account compliance (not breached, active status), and returns only setups that are safe to execute. This is the orchestration layer that sits between strategy evaluation and order execution — it ensures that only compliant setups reach the executor. Endpoints: `POST /v1/strategy-orchestrator/evaluate` (accepts a `MarketSnapshot`, returns an `OrchestrationResult` with per-account evaluations and summary stats). Covered by `backend/tests/test_strategy_orchestrator.py` (10 tests). Full suite green: 4345 passed, 0 failed (4335 + 10 new).
- **Instagram content manager is now real and wired (2026-08-31):** `backend/app/instagram_content/` at `/v1/instagram` (branch `feature/instagram-content-manager`, PR #926) — propose → human decision (approve/reject, can edit caption) → publish. Publish only succeeds from `approved` status and is the one real execution boundary: it calls a single configurable n8n webhook (`N8N_INSTAGRAM_WEBHOOK_URL`) over a new shared external Docker network (`jarvis_shared`, see `docs/n8n-instagram-setup.md`) so an independently-run n8n container can perform the actual Meta Graph API publish without AURON ever holding those credentials. Replaces the disconnected `backend/app/content/` ceremony (11 modules) removed in the same PR. Covered by `backend/tests/test_instagram_content_manager.py`.
- **Note:** `feat/setup-submission` and `feat/strategy-orchestrator` were built in parallel by different sessions on slightly different bases and both merged; `setup_submission` and `strategy_orchestrator` are complementary (submission is the bridge to the approval inbox, orchestrator is the compliance-aware evaluation layer) and can both stay, but should be reconciled into one flow in the executor-wiring PR below rather than left as two independent paths into the same approval gate.
- Next: add `smc` and `open_range` strategies to the registry (same pattern; `open_range` will require extending `MarketSnapshot` with opening-range high/low fields), then in a later gated PR wire strategy assignments + account compliance + the existing approval-gated MT5 executor to enable the full trade flow (strategy generates setup → compliance check → approval gate → executor places order), reconciling `setup_submission` and `strategy_orchestrator` into that single path. Continue extending the Research allowlist deliberately as real needs arise; consider real wiring for Communications, Automation, and Documents verticals following the same pattern (one real endpoint, explicit safety boundaries, audit log).

- **Legacy ceremony wave reconciled (2026-08-31):** A full static reachability audit from `backend/app/main.py` (every `.py` file under `backend/app`, not just `auron_*`-named ones) found 855 of 1991 backend files (43%) unreachable from the application entrypoint. These split into two very different categories, and only one was deleted:
  - **Deleted (604 files + 297 paired tests, 901 total):** the remainder of the `core/auron_integration_readiness_vNNN.py` chain tail, plus a much larger and older ceremony wave spread across `app/api/`, `app/contracts/`, `app/schemas/`, `app/services/` — the `recovery_reliability_*`/`baseline_*`/`quarantine_*`/`consumer_adoption`/`reintegration_*` cluster (PHOENIX v21.170s-230s) and the `auron_demo1_telegram_*_successor_next_generation_*_restoration/monitoring/continuity` chain in `app/api/routes/`. Confirmed zero callers outside each cluster's own tests, and zero dependency from the modules kept below onto anything in this set, before deletion. Full suite green afterward: 2349 passed, 0 failed (down from 4326 as the removed clusters' own tests went with them).
  - **Kept, not wired yet (`app/modules/` 150 files, `app/phoenix/v21_5X_*` 42 files, `app/executive_*/` 60 files across 20 packages, `app/semantic_graph/` 2 files — 254 files total):** these are self-contained packages (`models.py`+`service.py`+`router.py` or `api.py` per module, no `_vNNN` chained lineage), several already covered by real, passing tests in the suite (e.g. `trade_scenario_visualizer`, `self_learning_performance_optimizer`, `outcome_verification_engine`). This is real, substantial, unwired functionality — risk engines, position management, execution supervision, trade journaling, treasury/liquidity governance among them. Wiring these into `main.py` is tracked as the next piece of work, prioritizing the pieces most relevant to live trading (risk, position management, execution supervision) first.
  - `app/memory/storage.py` (an in-memory fallback store, superseded by `app/memory/sql_storage.py`) was left alone: trivial, not part of either pattern, not worth bulk-processing.
- Next: wire the highest-value modules from the kept-but-unwired set into `main.py` (risk/position/execution first), one coherent PR per module or tightly related cluster, same standard as every other reconciliation in this log: real test coverage, full suite green, no phase-counter advancement without a working `main.py` entrypoint behind it.

- **Legacy real modules wired (2026-08-31):** The 254 files identified above as "kept, not wired" have been registered in `main.py` — 83 routers across `app/modules/`, `app/phoenix/v21_5X_*`, `app/executive_*/` (risk engine, position management, execution supervisor, trade journal intelligence, treasury/liquidity governance, KPI/roadmap/priority engines, crisis/incident/recovery response, and more; `app/semantic_graph/` has no router, it's a library used internally). Found and fixed along the way: a real bug in `trade_journal_intelligence/models.py` (deprecated pydantic `regex=` kwarg, never caught before because the module had never actually been imported by a running app), and two genuine new route collisions this wiring introduced (`autonomous_executive_orchestrator` and `phoenix/v21_53_strategic_portfolio` both defaulted to prefixes already used elsewhere — repointed to `/v1/autonomous-executive-orchestrator` and `/v1/phoenix/strategic-portfolio`). Also found, but explicitly NOT fixed here (pre-existing, unrelated to this PR, present before any wiring): 12 route collisions already in `main.py` between the demo1 `v21.255`/`v21.256` dialogue endpoints and a duplicate registration inside `executive_mt5_portfolio_correlation_exposure` -- flagged as a follow-up. Full suite green: 2349 passed, 0 failed (app now serves 2781 total routes).
- Next: fix the 12 pre-existing route collisions noted above; then decide whether the wired-but-still-standalone modules (risk engine, position management, execution supervisor, etc.) should be integrated into the live trade flow (strategy → compliance → approval gate → executor) alongside `setup_submission`/`strategy_orchestrator`, or kept as independent read/analysis surfaces for now.

- **Route collisions fixed (2026-08-31):** the 12 pre-existing duplicate-route registrations flagged in the previous entry are resolved. Root cause: several `auron_demo1_*` modules "borrow" a predecessor's routes via `router.routes.extend(other_router.routes)` in addition to `main.py` separately, directly `include_router`-ing that same predecessor -- so anything reachable both ways got registered twice. Fixed by removing the redundant merge in `auron_demo1_conversation_memory_v21_243.py` (was re-absorbing v21.255, and transitively v21.256, on top of their own direct registrations) and the redundant nested include in `executive_decision_engine/api.py` (was re-including `executive_mt5_portfolio_correlation_exposure_router` on top of its own direct registration in `main.py`). Left the v21.255→v21.256 merge itself alone: it's v21.256's only path into the app (never directly included), so removing it would have deleted the endpoint rather than deduplicating it. Removing the v21.243 merge changed import order enough to surface a latent circular import (v21.257→v21.256→v21.255→v21.256); fixed by moving v21.255's import earlier in `main.py`, ahead of v21.257, restoring the ordering the old merge used to provide as a side effect. Full suite green: 2349 passed, 0 failed, 0 route collisions remaining (2781 unique routes).

- **Real integration started: dynamic_risk_engine wired to the live setup flow (2026-08-31):** `backend/app/trade_risk_pipeline/` at `POST /v1/trade-risk-pipeline/assess/{approval_request_id}` -- the first real link between an approved `setup_submission` setup and one of the modules wired-but-unintegrated in the previous entry. Pulls the pending/approved setup and the account's live balance/equity/prop-firm rules from the account registry, builds a real `DynamicRiskCreate` (mapping buy/sell -> long/short, deriving `setup_grade` from strategy confidence, account risk snapshot from live balance/equity/day-start-balance, policy bounds from the account's actual `PropFirmRules`), and returns dynamic_risk_engine's real, stop-distance-and-risk-percent-based position size -- directly targeting the position-sizing/oversized-entry gap flagged repeatedly in past trading review. Fails closed on a suspended/breached account (checked independently of, and in addition to, the account registry's own breach detection). Does not open a position or place an order -- output is a sizing recommendation, one more gate before any real execution. `value_per_price_unit` (pip/point value) must be supplied by the caller; AURON does not yet track per-instrument contract specs, so the default of 1.0 is a sanity-check value only, not for real sizing. Covered by `backend/tests/test_trade_risk_pipeline.py` (4 tests: unknown approval fails closed, real stop-distance-derived sizing verified by formula not just presence, suspended-account fails closed, already-breached account fails closed one gate earlier via the account registry itself). Full suite green: 2353 passed, 0 failed.
- Next: same pattern for `position_management_brain` (open/track the sized position with its exit rules) and `execution_supervisor` (health/telemetry once a position is being managed) -- one PR each, following this one's shape (pull real upstream state, map into the target module's actual schema, fail closed, real tests with a genuine formula/behavior check not just a status-code check). `value_per_price_unit` per instrument is a real gap to close before this pipeline's output can be trusted for anything beyond a sanity check.

- **Second real link: position_management_brain wired to a risk-approved setup (2026-08-31):** `POST /v1/trade-risk-pipeline/accounts/{workspace_id}/open-position/{risk_record_id}` opens a real, tracked position from a `dynamic_risk_engine` record that is in the `risk-approved` state (refuses anything else). Uses the risk engine's own computed `recommended_position_units`/`recommended_risk_amount` as the position's actual size and risk amount -- not re-derived, not re-guessed. Maps the strategy's take-profits into real `position_management_brain` exit rules, and along the way found and fixed a genuine schema mismatch: `setup_submission`'s `TakeProfit.close_pct` is *cumulative* (e.g. 30/50/100) while `position_management_brain`'s `ExitRule.close_percent` is *incremental* and must not sum past 100 -- silently passing cumulative values through would have either been rejected outright or, worse, closed far more of a position than intended. Fixed with an explicit, independently-tested conversion (`_exit_rules_from_take_profits`). Still does not place a broker order -- a position record here means "AURON is now tracking this as an intended, sized, risk-approved position with real exit rules," not "MT5 has an open trade." Tests: 8 in `test_trade_risk_pipeline.py` (up from 4), including the take-profit conversion tested directly and deterministically (not via heuristics that might not trigger). Full suite green: 2357 passed, 0 failed.
- Next: `execution_supervisor` (health/telemetry for a position once opened) is the remaining piece from the original three; after that, the real gap before any of this can safely reach a broker is still the `value_per_price_unit` per-instrument contract-spec gap noted above.

- **Third real link, chain complete: execution_supervisor wired to opened positions (2026-08-31):** `POST /v1/trade-risk-pipeline/accounts/{workspace_id}/positions/{position_id}/start-supervision` starts real `execution_supervisor` health tracking for an already-opened `position_management_brain` position, modeling it as a one-stage workflow (`position-lifecycle`) with the position's actual state/remaining-percent as telemetry. Refuses positions in a closed/blocked/invalidated/archived state and refuses starting supervision twice for the same position (duplicate `source_key`). Completes the chain planned across #930/#931/this PR: `setup_submission` (approved setup) -> `dynamic_risk_engine` (real, stop-distance-based position size) -> `position_management_brain` (tracked position with real exit rules, cumulative->incremental take-profit conversion fixed) -> `execution_supervisor` (ongoing health monitoring). None of the four steps places a broker order; each is a real, tested gate with its own fail-closed state machine, not a rubber stamp. Tests: 12 in `test_trade_risk_pipeline.py` (up from 8), including driving a position through its own real approve->mark-open->close lifecycle to verify supervision correctly refuses a closed position. Full suite green: 2361 passed, 0 failed.
- **Known real gaps before any of this reaches a broker**, carried forward from earlier entries and not yet closed: (1) `value_per_price_unit` (pip/point value per instrument) has no real source yet -- caller-supplied, defaults to a sanity-check-only 1.0; (2) `start-supervision`'s `timeout_seconds` defaults to a flat 24h because strategies don't yet carry their own max-hold-time metadata; (3) actually placing an order still requires the separate, still-not-built approval-gated MT5 executor wiring noted in the Trading phase entry above -- this pipeline produces a fully-governed, ready-to-execute position record, nothing more.
- Next: per-instrument contract-spec source for `value_per_price_unit` (likely from the MT5 account-state-sync bridge, which should already know symbol specs), or continue toward the actual MT5 executor wiring now that the upstream governance chain is real end-to-end.

- **Fourth link, the one closest to real money: executive_mt5_live_order_executor wired (2026-08-31):** `POST /v1/trade-risk-pipeline/accounts/{workspace_id}/positions/{position_id}/prepare-live-order` runs live-order preflight (quote freshness, price deviation, volume/stop validity, risk limits) for a planned/approved position, using a real, previously-unwired module (`executive_mt5_live_order_executor`) that already had a complete preflight/execute/audit API and a Windows-only `MetaTrader5` package boundary -- it just wasn't reachable from `main.py`. Found and fixed the same duplicate-registration pattern as PR #929 while wiring it in: `executive_decision_engine/api.py` was also nesting this router, causing 6 more route collisions; removed the redundant nested include the same way. **This step never places an order.** It only calls the executor's `create()` (preflight), never `execute()`, and never sets `human_approved=True` -- that remains a separate, explicit call outside this pipeline by design, so no automated chain can end in a live trade. Even with that separate call, nothing reaches a real account unless AURON is running somewhere with an actual, logged-in MT5 terminal and the `MetaTrader5` Python package -- neither exists in the development/CI environment, so this is inert everywhere except real trading infrastructure. Only the initial stop-loss is carried into the order; partial take-profits remain `position_management_brain` exit rules applied after the position is open, not part of the initial order send. Quote data, symbol point size, and volume limits are still caller-supplied every time -- no real live-quote or contract-spec source exists yet (same gap noted for `value_per_price_unit` above). Tests: 17 in `test_trade_risk_pipeline.py` (up from 12), explicitly verifying `human_approved` always comes back `False`, the order never reaches `EXECUTED`/`SUBMISSION_PENDING`, and `native_adapter_ready` defaults to `False` (never silently "ready"). Full suite green: 2366 passed, 0 failed.
- **This PR is not auto-merged.** Every other reconciliation/wiring PR in this log was merged directly once its own suite was green; this one touches the boundary closest to live order placement and is left for explicit human review before merging, even though nothing in it can execute a trade on its own.
- Next: same real-data gaps as before (live quotes, per-instrument contract specs) need a real source before this chain is trustworthy end-to-end; until then, every number `prepare-live-order` uses is caller-supplied and must be verified against the actual broker/terminal by a human before ever calling `/execute`.

- **Live-quote gap closed for `prepare-live-order` (2026-08-31):** `LiveOrderPrepareRequest.quote_bid`/`quote_ask`/`quote_age_seconds` are now optional. When all three are omitted, `trade_risk_pipeline` looks up the freshest real tick from the `mt5_bridge` terminal matched to the account by `(login, server)` -- the same matching `account_state_sync` already uses -- for the position's symbol, and fails closed with a clear error if no matching terminal is registered or no recent tick exists for that symbol, rather than inventing a quote. Passing all three explicitly still works (e.g. for testing, or before a bridge is connected). Partial overrides (only one or two of the three) are explicitly rejected rather than silently guessing the rest. This closes half of the "no real data source" gap noted in the previous two entries -- the remaining half, per-instrument contract specs (`value_per_price_unit`, `symbol_point`, volume limits), still has no real source and is still caller-supplied. Tests: 21 in `test_trade_risk_pipeline.py` (up from 17): real tick lookup end-to-end via a registered `mt5_bridge` terminal, fail-closed with no terminal registered, fail-closed with a terminal but no tick for the symbol, and rejection of partial quote overrides. Full suite green: 2370 passed, 0 failed.
- Next: no real source yet for per-instrument contract specs (point size, volume step/min/max). `mt5_bridge`'s ingest schema doesn't carry symbol specs at all currently -- closing this gap for real would mean extending `MT5SnapshotIngest` (and whatever bridge script eventually pushes to it) with a symbol-info payload, not something derivable from data already flowing through the bridge.

- **Both real-data gaps closed: per-instrument contract specs now sourced from mt5_bridge (2026-08-31):** `mt5_bridge`'s ingest schema gained `MT5SymbolSpec` (point, digits, volume_min/max/step, contract size, tick size/value -- exactly what MT5's own `symbol_info()` reports), replacing by symbol on each ingest rather than accumulating. `assess()`'s `value_per_price_unit` and `prepare-live-order`'s `symbol_point`/`min_volume`/`max_volume`/`volume_step` are now all optional: when omitted, both look up the real spec from the mt5_bridge terminal matched to the account, deriving `value_per_price_unit = trade_tick_value / trade_tick_size` from real broker-reported tick economics rather than a guessed constant. Fails closed with a clear error if no matching terminal or no spec for the symbol exists -- same discipline as the tick lookup in the previous entry. Explicit overrides still work for all fields, and partial overrides (some but not all of a group) are still rejected. This closes the second half of the gap noted in the last three entries; the `value_per_price_unit=1.0` sanity-check default from the very first `trade_risk_pipeline` entry is gone -- real sizing now runs on real contract economics when a bridge is connected, or fails outright rather than silently using a placeholder. Tests: 25 in `test_trade_risk_pipeline.py` (up from 21): real end-to-end spec lookup for both `assess` and `prepare-live-order`, fail-closed with a tick present but no spec, and fail-closed with no terminal at all. Full suite green: 2374 passed, 0 failed.
- **The trade-risk pipeline chain is now real end-to-end with no known placeholder data left in its own code**: `setup_submission` -> `dynamic_risk_engine` (real sizing) -> `position_management_brain` (real tracked position) -> `execution_supervisor` (real health tracking) -> `executive_mt5_live_order_executor` preflight (real quote + real contract spec, never auto-executes). The only remaining gap to an actual live trade is infrastructure, not code: AURON has to actually run somewhere with a real MT5 terminal logged in and a bridge script pushing account/tick/symbol data to `mt5_bridge`'s ingest endpoint -- none of which exists in this development/CI environment by design.

- **The infrastructure gap: MT5 bridge pusher script built (2026-08-31):** `bridge/mt5_pusher.py` (+ `bridge/README.md`, `bridge/requirements.txt`) is the piece meant to actually run on a Windows machine next to a logged-in MT5 terminal, closing the last gap noted in the previous entry. Strictly read-only against MT5: only calls `account_info`, `positions_get`, `orders_get`, `history_deals_get`, `symbol_info`, `symbol_info_tick` -- never `order_send`/`order_check`. Registers itself with `mt5_bridge` once (persisting the returned `terminal_id` to a local state file for reuse), then loops on a configurable interval: heartbeat, collect a full snapshot (account, positions, pending orders, recent deals, ticks and symbol specs for the configured symbol list), push to `/v1/mt5/terminals/{id}/snapshot`. Cannot run in this environment (Windows-only package, needs a real terminal), so instead of leaving it untested, `backend/tests/test_mt5_bridge_pusher_schema.py` mocks the `MetaTrader5` module's surface and validates the script's actual output against the real `MT5SnapshotIngest` Pydantic model end-to-end -- a field-name drift between the script and the backend schema now fails a test instead of only being discoverable against a real broker. Also caught and handled: MT5 reports `sl`/`tp` as `0.0` when unset, not `None` -- the script converts, tested explicitly. Full suite green: 2376 passed, 0 failed.
- **The trade-risk chain is now complete on both sides**: real governance logic in the backend (this whole section) and a real, schema-verified way to feed it live broker data (this entry). What remains is purely operational: Brano running `mt5_pusher.py` on the machine with his MT5 terminal, pointed at wherever AURON's backend actually runs.

- **Instagram content manager gains a real moderation gate (2026-08-31):** `backend/app/instagram_content/moderation.py` runs explicit, deterministic policy checks on every proposed candidate before it ever reaches the human approval queue: caption length/emptiness, Instagram's 30-hashtag hard limit (with a warning at 25+), a spam-pattern phrase list, a hard aesthetic-score floor (auto-reject below 0.35), a soft aesthetic-score warning band (0.35-0.6, still goes to review but flagged), and near-duplicate-caption detection against everything already proposed in the running session. Violations auto-reject into a new `moderation_rejected` status -- never shown to the human as pending, so review time goes to actual judgment calls instead of catching spam patterns by hand. Warnings still reach `proposed` but are attached and visible. A human can always override an automated rejection via the existing `/decision` endpoint (now accepts `moderation_rejected` as well as `proposed`); doing so is explicitly logged as a human override in the candidate's audit trail, not silently absorbed. Tests: 18 new (`test_instagram_moderation.py`, 12; existing content-manager tests, 6 re-verified), covering each rule independently and the full propose -> moderation-reject -> human-override flow. Full suite green: 2387 passed, 0 failed.
- Next for this vertical: the moderation rules are static/hand-written policy, not yet informed by real posting history or engagement data; and there's still no automated candidate *sourcing* (something that actually looks at a Drive folder and proposes candidates) -- `propose()` still requires the caller to supply image_source_ref/caption_draft/aesthetic_score, matching the n8n-side responsibility split documented in `docs/n8n-instagram-setup.md`.

- **Instagram content manager gains real format/edit/hook planning (2026-08-31):** Candidates now carry one or more `MediaItem`s (image or video) instead of a single image, and `propose()` runs three new, deterministic decision passes before a candidate reaches review:
  - `format_decision.py`: exactly one video -> `reel` (Instagram now treats standalone video posts as Reels); exactly one image -> `single_image`; 2-10 items of any mix -> `carousel` -- Instagram's own structural rules, not a house opinion, each with the reasoning recorded on the candidate.
  - `edit_plan.py`: one `EditInstruction` per media item -- target aspect ratio (`4:5` feed / `9:16` Reel) and one consistent account-wide color-grade preset name (`auron-warm-mystic-v1`), so the feed reads as curated rather than randomly filtered per post. For over-length video (>90s), flags `trim_needed=True` and recommends a target duration band, but **never invents `trim_start_seconds`/`trim_end_seconds`** -- selecting the actual best segment of a video needs a human or a real video-content-analysis capability, neither of which exists; inventing timestamps here would have been worse than not trimming at all.
  - `hook.py`: warns (never hard-rejects) on the caption's opening line -- too short, opens with a hashtag, exceeds Instagram's ~125-char in-feed truncation, or reads as all-caps shouting.
  - `posting_schedule.py` + `GET /v1/instagram/posting-schedule/{weekday}`: generic, well-documented high-engagement windows by weekday -- explicitly labeled as *not* this account's own analytics, since there is no Instagram Insights integration yet.
  - `moderation.py` updated to score across all media items in a candidate (average against the aesthetic bar, plus a warning if any single item in a carousel is notably weaker than the rest).
  - The n8n webhook payload (`publisher.py`, `docs/n8n-instagram-setup.md`) now carries the full media list, the format decision, and the edit plan, so n8n's workflow has everything it needs to execute real cropping/grading/posting -- AURON still never touches pixels or holds Drive/Meta credentials.
  Tests: 30 across `test_instagram_content_manager.py` (rewritten for multi-media) and `test_instagram_moderation.py` (updated fixture) -- format decisions for all four media combinations, the carousel 10-item cap and video-duration validation enforced at the model level, edit-plan consistency and the "never invent a trim window" guarantee, hook warnings, and the full n8n payload shape. Full suite green: 2399 passed, 0 failed.
- **Explicit scope boundary, stated here so it isn't re-litigated per PR**: AURON decides *what* should happen to content (format, crop target, grade preset, trim need, posting window) as a structured spec. It does not download files, does not run image/video processing, does not hold Drive or Meta credentials, and does not pick which 15-30s of a longer video is the best moment -- those stay n8n's job (execution + credentials) or a human's job (creative judgment on trim selection), matching the master plan's own simulation/execution split.

- **Media pool + curation engine: the "know what a high-end account looks like" piece (2026-08-31):** `backend/app/instagram_content/media_pool_models.py` + `media_pool_service.py` + `curation.py` add a real, automated content-generation pipeline on top of the format/edit/hook work above, closing the gap between "you give AURON a folder of 100-200 photos" and "you just approve finished posts":
  - **Media pool**: `POST /v1/instagram/media-pool/ingest` accepts analyzed photos/videos (`theme`, `tags`, `aesthetic_score`, from whatever vision-analysis step runs where the files live -- AURON does not analyze pixels itself, same boundary as everywhere else in this vertical). Deduplicated by `media_ref`.
  - **Curation (`curate()`)**: real, explicit, non-black-box rules for what a high-end account's carousels/posts should look like -- a video always stands alone as a Reel; an image scoring above an elite threshold (0.85) gets its own hero post rather than being diluted into a group; same-theme images batch into right-sized carousels (3-6 items, Instagram's own sweet spot for swipe-through completion, never a thin 2-item carousel); a leftover of 1-2 same-theme images is deliberately left unposted rather than forcing a weak set. **Every media item can only ever end up in one post** -- draft creation reserves items out of the pool immediately, so a second curation run never proposes the same photo twice; a discarded draft (wrong grouping) cleanly returns its items; a finalized draft (real candidate, cleared moderation) marks them permanently used.
  - **`POST /v1/instagram/curate` -> `GET /curate/drafts` -> `POST /curate/drafts/{id}/finalize`**: the automated flow. Finalize attaches a caption and runs the draft through the existing moderation/format/edit-plan pipeline; a moderation rejection (bad caption) leaves the draft pending for a retry rather than burning the photos.
  - **Deliberately not built here**: AURON still does not write the actual caption/hashtag text. That stays a separate step (the existing n8n Claude-based captioning workflow) feeding `finalize` -- writing captions is a different capability (LLM generation with account voice/credentials) from curation (grouping/format/policy decisions), and duplicating it inside AURON wasn't asked for build without an explicit decision on where that capability should live. `docs/n8n-instagram-setup.md` documents the full automated path including this handoff point.
  - `moderation.py`'s hashtag check gained a soft optimal-range warning (3-8 tags, well-documented current guidance for reach on curated accounts) alongside the existing 30-tag hard limit and a "zero hashtags is a missed opportunity" warning.
  Tests: 13 in `test_instagram_media_pool.py` (curation grouping rules individually, pool reservation/dedup, the full finalize-succeeds and finalize-fails-leaves-photos-available paths) + 1 existing moderation test updated for the new hashtag guidance. Full suite green: 2412 passed, 0 failed.
- **Open decision, not resolved here**: should AURON gain its own direct caption-generation capability (a real Anthropic API call from the backend, needing its own API key) so the whole loop closes without an n8n round-trip for captions, or should captioning stay with n8n's existing Claude step as documented above? Flagged for Brano rather than assumed.

- **AURON writes its own captions now: the open decision above is resolved (2026-08-31):** `backend/app/instagram_content/caption_writer.py` (`AnthropicCaptionWriter`) makes a real, bounded HTTPS call to Anthropic's own Messages API (`ANTHROPIC_API_KEY` from environment, no fallback, no default) to generate the caption + hashtags for a curated draft directly in the account's voice (a configurable brand-voice prompt, defaulting to "confident, understated, slightly mystical... build in silence, let results speak"), respecting the 3-8 hashtag range and no-engagement-bait rules `moderation.py` already enforces. `finalize_draft`'s `caption_draft` is now optional: omit it and AURON generates one itself; supply it and generation is skipped entirely (still useful for one-off manual posts). This is the direct replacement for the n8n-Claude round trip documented in the previous entry, not an addition on top of it -- Brano chose this over keeping captioning in n8n. Fails closed exactly like everywhere else in this build: no API key, a transport error, an API error status, or an empty response all raise `CaptionWriterError` -> `InstagramContentError`, and critically **before** any pool items are touched, so a failed generation attempt never consumes photos. `docs/n8n-instagram-setup.md` updated: n8n is no longer part of the captioning path, only the final Meta Graph API publish step. Tests: 8 in `test_instagram_caption_writer.py` (no-API-key fail-closed, real request shape sent to Anthropic including brand voice and hashtag-range instructions, API-error and empty-response fail-closed, the full finalize-without-caption path, explicit-caption-skips-generation, and the caption-failure-does-not-consume-photos guarantee). Full suite green: 2420 passed, 0 failed.
- **The Instagram vertical's full automated loop is now real end-to-end**: photo folder -> vision-analysis ingest (external) -> `curate` -> `finalize` (AURON writes the caption itself) -> human approval -> `publish` (n8n, Meta Graph API). The only manual step left in the intended flow is the human approval gate, which is deliberate, not a gap.

- **AURON actually looks at the photos now: real vision analysis (2026-08-31):** `backend/app/instagram_content/vision_analysis.py` (`AnthropicVisionAnalyzer`) makes a real Claude vision call (same `ANTHROPIC_API_KEY` as the caption writer) to derive `theme`, `tags`, and a real `aesthetic_score` directly from looking at a photo -- composition, lighting, coherence with the account's aesthetic -- rather than requiring those as pre-computed input. `POST /v1/instagram/media-pool/analyze-and-ingest` (`analyze_and_ingest.py`) orchestrates this over a batch: AURON still never fetches the file itself (no Drive credential), the caller supplies image bytes (base64) or an already-fetchable URL; each item's outcome (analyzed + ingested, or a specific failure reason -- missing video duration, a bad/unparseable model response, an API error) comes back individually, so one bad photo in a 100-200-item batch never blocks the rest. Fails closed exactly like the caption writer: no API key, a non-JSON response, a response missing `theme`/`tags`/a valid `aesthetic_score`, or an API error all raise rather than fabricating a score. Video is analyzed via a representative still-frame thumbnail only -- Claude's vision reads a single image, not motion or audio, so a video's derived theme/score is explicitly a proxy from that one frame, not real video-content understanding (same honest boundary as the never-invented trim window from the format/edit-plan entry above). The plain `media-pool/ingest` endpoint (pre-analyzed input, no vision call) is unchanged and still available. `docs/n8n-instagram-setup.md` updated: n8n's Instagram role is now three purely mechanical steps -- fetch bytes from Drive, forward to AURON, and (after human approval) call the Meta Graph API to post. Every judgment call (what a photo is, whether it fits the account, how to group it, what to write about it) now happens inside AURON. Tests: 13 in `test_instagram_vision_analysis.py` (no-key fail-closed, missing-image-source fail-closed, real parsed result, real request shape including the image block and aesthetic criteria, both base64 and URL image sources, markdown-fence tolerance, invalid-JSON and out-of-range-score fail-closed, API-error fail-closed, full batch success, per-item video-duration failure isolation, per-item analysis-failure isolation, and the API route itself). Full suite green: 2433 passed, 0 failed.
- **The Instagram vertical is now fully AURON-native for every judgment call**: photo bytes in (from n8n, mechanically) -> AURON looks at each one and scores it -> AURON curates hero posts/carousels/reels -> AURON writes the caption -> human approves -> n8n posts (mechanically). The only remaining external dependency is file access (Drive credentials) and the final Meta Graph API call, both deliberately kept outside AURON per the master plan's credential-boundary principle.

- **Curation/moderation constants corrected against real, current 2026 platform data (2026-08-31):** Brano asked whether the curation rules were grounded in real research or just assumption -- they weren't, so a web search was run before touching any more code. Two of the hardcoded constants were meaningfully wrong:
  - **Hashtags**: Instagram enforces a **hard 5-hashtag cap platform-wide since December 2025** (publish-time restriction across posts, Reels, and comments combined -- not a style guideline). The code had `MAX_HASHTAGS = 30` (the old, pre-cap ceiling) as a hard violation threshold and a `(3, 8)` "optimal" range. Corrected to `MAX_HASHTAGS = 5` and `OPTIMAL_HASHTAG_RANGE = (3, 5)`, matching Meta's own current official guidance; Instagram's own head has stated hashtags no longer drive reach directly, only content categorization. `moderation.py` and the `caption_writer.py` prompt both updated.
  - **Carousel size**: 2026 engagement data consistently shows a **7-10 slide sweet spot** (highest average engagement rate, highest dwell time, strongest algorithm reshow bonus for swipe-through), not the 3-6 range the curation engine had been using. `curation.py`'s `CAROUSEL_IDEAL_MAX_SIZE` corrected from 6 to 10; `CAROUSEL_MIN_SIZE` (3) is still consistent with current guidance for quick-hit/motivational formats and was left unchanged.
  - Both constants now carry a dated comment (2026-08-31) noting they were verified via web search and that platform rules change, so they should be re-verified rather than assumed correct indefinitely.
  - Tests updated to match the real cap (hashtag-count tests now check against 5/6, not 27/31); curation tests were already parameterized off the constants and needed no changes. Full suite green: 2433 passed, 0 failed.
- **Standing practice going forward**: any hardcoded platform-behavior constant in this vertical (limits, sweet spots, algorithm-favored ranges) should be sourced from a current search before being trusted, not assumed from general knowledge -- platform rules like the hashtag cap change with real, dated announcements that predate what any static knowledge would reflect.

- **AURON gets its own ongoing web-research capability, with a hard rule: research never silently changes behavior (2026-08-31):** Brano asked for AURON to be able to refresh its own trend/pattern knowledge rather than relying on Claude's one-off research from the previous entry. Built with the same fail-closed, human-gated discipline as every other real capability in this build:
  - `web_research.py` (`TavilyWebResearcher`): a real, bounded HTTPS call to Tavily's search API (`TAVILY_API_KEY` from environment, no fallback) -- built specifically for AI agents (pre-filtered, full-page-read results plus a synthesized answer, not raw search-engine snippets). `POST /v1/instagram/research/query` exposes this for ad-hoc research; purely informational.
  - `platform_strategy.py`: the hashtag-cap/range and carousel-size values moderation.py and curation.py enforce are no longer hardcoded module constants -- they live in one small `PlatformStrategy` object held by `PlatformStrategyStore`, seeded with the values verified in the previous entry. `moderation.py`, `curation.py`, and `caption_writer.py` were refactored to read the *live* store on every call, so a change takes effect immediately once applied -- no code deploy needed. Aesthetic-score thresholds stay as fixed local values in `moderation.py`/`curation.py`'s `ELITE_SOLO_THRESHOLD`, deliberately -- they're account-taste calls, not platform facts a web search can verify.
  - `research_synthesizer.py` (`ResearchSynthesizer`): a real Anthropic call that turns Tavily research into a structured, cited `PlatformStrategyProposal` (specific values + reasoning + source URLs) -- explicitly instructed to keep a value unchanged rather than invent a number the research doesn't support.
  - **The proposal is never auto-applied.** `POST /v1/instagram/research/platform-rules/refresh` returns a proposal; a separate, explicit `POST /v1/instagram/research/platform-rules/apply` call (with the reviewed values) is the only way the live store actually changes -- same "propose, then a separate explicit act" pattern as `dynamic_risk_engine`'s live-order preflight in the trading vertical. `GET /v1/instagram/research/platform-rules` shows what's currently enforced and why.
  Tests: 11 in `test_instagram_web_research.py` -- search fail-closed/real-request-shape/API-error, synthesis fail-closed/missing-field/invalid-JSON, the store's apply-only-explicitly guarantee (a fresh snapshot is provably untouched by `synthesize()`), and moderation actually reflecting a live-applied strategy change end-to-end. Full suite green: 2444 passed, 0 failed.
- **Full picture of the Instagram vertical's external dependencies now**: Anthropic (`ANTHROPIC_API_KEY`, captions + vision + research synthesis) and Tavily (`TAVILY_API_KEY`, web search) are both real, held directly by AURON. Drive file access and the final Meta Graph API publish stay outside AURON by design (credential-boundary principle, unchanged).

- **Real video trim selection closes the last honest gap from the format/edit-plan work (2026-08-31):** `video_trim_analysis.py` (`AnthropicVideoTrimAnalyzer`) picks a real highlight window for an over-length Reel from actual sampled frames, instead of leaving `trim_start_seconds`/`trim_end_seconds` permanently unset. The caller (n8n) extracts several still frames at a regular interval across the video and supplies them with real timestamps via `POST /v1/instagram/media-pool/{item_id}/analyze-trim`; AURON sends all frames to Claude in one call, each labeled with its actual timestamp, and asks for the strongest contiguous window. **Critical safety property, directly tested**: the response is validated to fall within the actually-sampled timestamp range before being trusted -- a window outside what was sampled (a hallucinated timestamp) is treated as an invalid response and raises `VideoTrimAnalysisError`, never silently clamped into something that looks plausible. The result is stored on the `MediaPoolItem` (`recommended_trim_start_seconds`/`_end_seconds`/`trim_reasoning`) and flows automatically into `edit_plan.py`'s output the next time `/curate` -> `/finalize` runs for that item -- `build_edit_plan` now sets a real trim window when one has been analyzed, and otherwise still flags `trim_needed: true` with the window left unset for a human, exactly as before. `models.MediaItem` and `EditInstruction`'s docstrings updated to reflect that trim timestamps are now sometimes real, never invented. Tests: 10 in `test_instagram_video_trim.py` -- fail-closed on no key and fewer than 3 frames, a real accepted window, every frame sent labeled with its real timestamp, the out-of-sampled-range rejection (the core safety guarantee), end-before-start rejection, invalid-JSON fail-closed, and both `edit_plan.py` branches (real window used when present, left unset otherwise). Full suite green: 2454 passed, 0 failed.
- **The "kann er Videos kürzen" question from earlier in this session now has a real answer**: yes, when given sampled frames with real timestamps -- AURON picks the window, it never invents one from duration alone.

- **Correction: Communications/Automation/Documents were already real, not still needing wiring (2026-08-31):** A repo-wide reachability re-scan (same method as the legacy-ceremony-wave audit) came back with only 2 unreached files total, both the intentionally-router-less `semantic_graph` library. Investigating the "real wiring for Communications, Automation, and Documents" item from an earlier entry found it was already done, just organized differently than expected: `automation_runtime` (job/connector orchestration -- Instagram/YouTube/Gmail/Calendar/Browser/Documents/Telegram connector types, queued/waiting-approval/ready job states), `document_intelligence` (document CRUD + analysis + audit trail), and `notification_hub` (multi-channel delivery -- dashboard/telegram/email/mobile-push/voice, with quiet-hours support) are all real, tested (`test_automation_runtime.py`, `test_document_intelligence.py`, `test_notification_hub.py`, all passing), and already wired into the live app -- nested under `market_vision/api.py`'s router rather than included directly in `main.py`, which is why they weren't obviously discoverable under the vertical names the master plan used. Three leftover empty directories (`app/communications/`, `app/automation/`, `app/documents/` -- no `.py` files, just stale `__pycache__`) from an earlier cleanup pass were removed; they were dead weight, not placeholders for anything still to be built. Full suite green: 2454 passed, 0 failed, confirming nothing was actually relying on the empty stubs.
- Next: no further action needed on this front unless real functionality gaps are found in `automation_runtime`/`document_intelligence`/`notification_hub` specifically (as opposed to assuming the verticals are unbuilt). The naming mismatch (real modules living under `market_vision` rather than top-level `communications`/`automation`/`documents`) is cosmetic, not a functional gap, and renaming/moving them for clarity is a discretionary cleanup, not a priority.

- **Instagram Command Centre dashboard (2026-08-31):** `GET /v1/instagram/dashboard` -- one real, aggregated view of the whole vertical, matching the master plan's original Command Centre intent (Punkt 2: chat/tasks/system-health/approvals all visible in one place) applied to this one vertical. Every number is computed live from the actual services on each call, nothing cached or estimated: candidates needing review vs. moderation-rejected-but-overridable vs. approved-and-ready vs. posted vs. failed; media pool available/used counts; pending curated drafts; the most recent candidates across all statuses; the platform strategy currently in effect (hashtag cap, carousel size, when/why last updated); and today's suggested posting windows. `backend/app/instagram_content/dashboard.py` is a thin, real aggregator over the existing services -- no new state, no new source of truth, just one place to see what actually needs a decision right now instead of querying `/candidates`, `/media-pool`, `/curate/drafts`, and `/research/platform-rules` separately. Tests: 5 in `test_instagram_dashboard.py` -- candidate-status counts reflect real state changes, media pool available/used counts and curated-draft reservation reflected correctly (including the real 3-item carousel minimum, not a dashboard assumption), recent-candidates ordering and limit, platform strategy and posting windows present, and the API route itself. Full suite green: 2459 passed, 0 failed.

- **Real notifications: closes the "ich bekomme nur benachrichtigt" loop from earlier in this session (2026-08-31):** `notification_hub` (a real, already-tested pre-existing module -- quiet hours, priority-based channel routing, acknowledgement tracking, `owner_name: "MASTER Brano"`) had one honest gap: its `_deliver()` step only simulated delivery, marking every channel "delivered" without actually sending anything. Two changes:
  - `backend/app/notification_hub/telegram_delivery.py` (`TelegramDeliveryClient`): a real, bounded HTTPS call to the Telegram Bot API (`TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` from environment, no fallback). `_deliver()` now actually sends to Telegram for that channel; other channels (dashboard, email, mobile_push, voice) remain honestly labeled "no real delivery adapter yet" rather than silently implying they're equivalent. A record is marked `delivered` if at least one configured channel actually succeeded, `failed` only if every configured channel failed -- a Telegram failure alone (e.g. no bot token set) doesn't block the record when another channel (like dashboard) is also configured.
  - `instagram_content/service.py`: `propose()` now calls `notification_hub_service.create()` for every candidate that reaches `proposed` status (not for moderation-auto-rejected ones, which aren't waiting on a human). This applies to both the direct `/candidates` path and `finalize_draft()` (the curated-pool path), so "AURON built a post, here's a notification" now actually happens instead of requiring Brano to poll the dashboard. Wrapped in a broad exception guard so a notification-delivery problem can never break candidate creation itself.
  Tests: 10 in `test_notification_hub_telegram.py` (fail-closed without credentials, real request shape to the Telegram API, API-error and `ok: false` fail-closed, real delivery wired into `_deliver` via dependency injection, a Telegram failure not blocking a record with another working channel, and a record failing when every configured channel fails) + 2 in `test_instagram_content_manager.py` (a proposed candidate creates a matching notification; a moderation-rejected one does not). Full suite green: 2468 passed, 0 failed.

- **Real email delivery, extending the same pattern to a second channel (2026-08-31):** `backend/app/notification_hub/email_delivery.py` (`SmtpEmailDeliveryClient`) adds real email delivery via standard SMTP (`SMTP_HOST`/`SMTP_PORT`/`SMTP_USERNAME`/`SMTP_PASSWORD`/`NOTIFICATION_EMAIL_FROM`/`NOTIFICATION_EMAIL_TO` from environment, no fallback) -- provider-agnostic rather than locked to one vendor API, works with Gmail app passwords or any transactional-email provider. `_deliver()` now actually sends for both `telegram` and `email` channels; `dashboard`/`mobile_push`/`voice` remain honestly labeled "no real adapter yet." Same fail-closed and partial-failure-tolerant behavior as the Telegram integration: missing settings or an SMTP-level error raise `EmailDeliveryError` rather than pretending success, and one channel failing doesn't block a record that has another working channel configured. Tests: 6 in `test_notification_hub_email.py` (fail-closed on missing settings, every missing setting named in the error, a real SMTP flow exercised via an injectable fake connection since a live SMTP client can't be exercised with `httpx.MockTransport`, login/send failures, STARTTLS toggle) + 1 in `test_notification_hub_telegram.py` confirming the email channel is wired into `_deliver` the same way Telegram is. Full suite green: 2475 passed, 0 failed.
- `notification_hub` now has two real, working delivery channels (Telegram, email) out of five defined; `dashboard`/`mobile_push`/`voice` remain advisory-only, honestly labeled, not silently implied to work.

- **Dashboard now reflects real notification activity, closing the loop with the previous two entries (2026-08-31):** `InstagramDashboard` gained `recent_notifications`, `notifications_delivered_today`, `notifications_failed_today` -- pulled live from `notification_hub` filtered to `domain == "instagram"`. This makes a real failure mode visible instead of silent: if Telegram/email credentials aren't configured (or fail), a candidate can still be `proposed` while its notification is `failed` -- the dashboard now shows that gap directly rather than only showing candidate counts and leaving "was I actually told?" unanswered. Tests: 2 new in `test_instagram_dashboard.py` (a proposed candidate produces a matching notification the dashboard surfaces; notifications from other domains are correctly excluded). Full suite green: 2477 passed, 0 failed.

- **document_intelligence gains real AI analysis, deliberately unblocked after checking with Brano first (2026-08-31):** Found a hard-coded validator (`"v8.4 permits deterministic local analysis only"`) blocking `AnalysisRequest.use_external_ai=True` entirely -- unlike every other credential-boundary decision in this build, this one looked like a deliberate prior design choice (documents can contain contracts/financial/legal/personal content), not an oversight, so it was raised with Brano before touching it rather than assumed. Clarified: the restriction blocked AURON's own analysis pipeline specifically (there is no third-party access it was protecting against), and Brano confirmed he wants AURON itself to have real analysis capability. Unblocked accordingly:
  - `backend/app/document_intelligence/document_ai_analyzer.py` (`AnthropicDocumentAnalyzer`): a real, bounded Anthropic call (same `ANTHROPIC_API_KEY` as the Instagram vertical) that reads the document's actual text and returns category/summary/risks/key-points -- opt-in only via `use_external_ai=True`, never the silent default, since document content leaving the process for this call is a materially different privacy posture than the local keyword/regex methods.
  - `service.py`'s `analyze()`: when requested, AI-derived category/summary/key-points take priority (real reasoning over the actual text); AI-flagged risks are merged with, not replacing, the deterministic regex-based risk findings, since the two catch different, complementary patterns. **Fails closed, not silently, on AI failure**: a new `_failed()` path (using the existing but previously-unused `AnalysisState.FAILED`) returns a record with `external_ai_used=True` and a clear `blocked_reason` rather than quietly falling back to the deterministic result and calling it a success -- if the caller asked for AI analysis, a failure must not look like a completed analysis.
  - `DocumentIntelligenceStatus.external_ai_execution` (previously hardcoded `False`, never actually reflecting the real block) now correctly reports `True`; version bumped `8.4` -> `8.5` to mark the real capability change.
  Tests: 9 in `test_document_intelligence_ai.py` (fail-closed without a key, real parsed result, real document text sent in the prompt, invalid-JSON/missing-field/API-error fail-closed, AI result used when requested, AI never called when not requested, and the critical fail-closed-not-silent-fallback guarantee) + 2 existing tests in `test_document_intelligence.py` updated to reflect the deliberate policy change (one renamed, one updated) rather than deleted. Full suite green: 2487 passed, 0 failed.
- **Standing note for future sessions**: not every hardcoded restriction found while exploring the codebase is dead ceremony to route around -- some are real, deliberate boundaries (this one, and the credential-boundary decisions elsewhere in this build). The distinguishing question is whether the restriction protects something (privacy, safety, a real decision) or just never got finished; when genuinely unsure, ask before unblocking, as happened here.

- **Instagram gains permanent memory via knowledge_graph (2026-08-31):** Another real, previously-unwired-to-anything module (`knowledge_graph` -- node/edge storage, text search, path-finding, already tested) put to use: every successful `publish()` now creates a real, permanent node (`_record_post_in_knowledge_graph`) recording the post's caption, hashtags, format, and media references. This is a genuinely different thing from `moderation.py`'s existing exact-caption duplicate check: that list lives only in the running `InstagramContentService` instance and is lost on restart; a knowledge_graph node persists and is searchable. `GET /v1/instagram/history/search?query=...` exposes it -- search by caption text, hashtag, or theme keyword to check "have I posted about this recently" before curating something new, filtered to `instagram`-tagged nodes so it doesn't surface unrelated graph data from other verticals. Wrapped in a broad exception guard, same as the notification hook: a knowledge-graph problem can never undo an already-successful publish. Tests: 3 new in `test_instagram_content_manager.py` (a successful publish creates a searchable node with the right tags/properties; a failed publish does not; the full API flow end-to-end including a mocked publisher since the real n8n webhook isn't reachable in this environment). Full suite green: 2490 passed, 0 failed.
- This is the third previously-built-but-unconnected module (after `document_intelligence`'s AI upgrade and `notification_hub`'s real delivery) put to real use this session rather than left idle. `automation_runtime` remains the one major real module not yet connected to anything outside its own tests -- a legitimate future candidate, but its pull-based job-dispatch model needs an external caller (cron, or n8n's own schedule trigger) to actually drive it, which is a design decision worth raising rather than assuming.

- **Real remote execution agent: the piece that actually closes the loop to a live broker (2026-09-02) -- built and tested live with Brano's own PUPrime demo account and MT5 terminal.** AURON's backend runs in a Linux Docker container and cannot load the Windows-only `MetaTrader5` package -- confirmed live: calling `/execute` with `human_approved=True` previously hit `RuntimeError("MetaTrader5 package is unavailable")` and returned `FAILED`, even though every check had already passed correctly. Fixed properly rather than worked around: `execute()` now leaves a fully-authorized order at `PREFLIGHT_READY` when no native executor is available, instead of failing -- this is not a fallback that fakes success, it's an honest "waiting for something that can actually submit this" state. New: `GET /v1/executive-mt5-live-order-executor/orders/pending-execution` (orders ready for a remote agent to pick up) and `POST /v1/executive-mt5-live-order-executor/orders/{id}/report-execution` (`RemoteExecutionReport` -- the real broker response, relayed verbatim). Both the original native-executor path and the new remote-report path now share one `_classify_broker_result` helper, so a real fill/rejection/reconciliation is classified identically regardless of which path produced it.
  `bridge/mt5_execution_agent.py`: the one component that can actually call `order_send()` for real. Polls `pending-execution` for one workspace, calls the real MetaTrader5 API, reports the raw broker response back. Makes zero trading decisions -- every decision (strategy, sizing, risk-approval, human-approval) already happened on the AURON side before an order can reach `preflight-ready`; the agent only executes what's already fully authorized. Requires `--i-understand-this-places-real-orders` on every run, no way to suppress it.
  Tests: 21 in `test_executive_mt5_live_order_executor.py` (up from 15 -- the new PREFLIGHT_READY-not-FAILED behavior, `pending_execution` listing only ready orders, `report_execution` applying the same classification as the native path including a real broker-rejection case, refusing a record not awaiting execution, unknown-record fail-closed) + 6 in `test_mt5_execution_agent_schema.py` (mirrors the `mt5_pusher.py` schema-verification pattern: mocks `MetaTrader5`, runs the agent's real `build_mt5_request`/`execute_one` functions, and validates the output against the real `RemoteExecutionReport` Pydantic model -- market buy maps to ask price, market sell to bid, an `order_check` rejection never reaches `order_send`, missing symbol info and an unexpected exception both produce a valid report instead of crashing the agent loop). Full suite green: 2512 passed, 0 failed.
- **This PR is not auto-merged, same as the original live-order-preflight PR (#933).** This is the component that can place a real order; Brano reviews it himself before it goes in, even though the chain leading to it (setup -> risk -> position -> supervision -> preflight) was already live-tested end to end today on his real demo account and produced a correctly risk-sized `risk-approved` position with real MT5 tick/contract data.

This checkpoint must be updated at each phase boundary or major activation milestone.
